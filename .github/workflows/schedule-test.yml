name: Schedule Test

on:
  schedule:
    - cron: "7,12,17,22,27,32,37,42,47,52,57 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test scheduler
        run: echo "Scheduled workflow fired at $(date -u)!" 
